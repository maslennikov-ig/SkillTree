# План исправления багов Telegram-бота SkillTree

## Контекст

Тестер прислал скриншоты с комментариями. Обнаружены UI/UX проблемы и критическая проблема с алгоритмом расчёта профориентации.

---

## Проблема 1: BINARY вопросы — текст ответов не виден

**Симптом**: В вопросах 7, 31, 45, 46, 49-51, 53, 54 ответы не видны.
**Причина**: Все эти вопросы типа `BINARY`. В `quiz.handler.ts:417-420` текст опций добавляется ТОЛЬКО для `MULTIPLE_CHOICE`, но не для `BINARY`.

**Файл**: `apps/bot/src/handlers/quiz.handler.ts`
**Строки**: 417-420

**Исправление**:
```typescript
// После строки 420, добавить:
if (question.type === "BINARY" && question.options) {
  messageText += `\n\n${formatOptionsAsText(question.options)}`;
}
```

---

## Проблема 2: Вопросы не жирным шрифтом

**Симптом**: Тестер просит сделать вопросы жирным.
**Анализ**: В коде уже есть `**${question.text}**` (строка 415), но возможно Markdown не парсится корректно для некоторых сообщений.

**Файл**: `apps/bot/src/handlers/quiz.handler.ts`
**Строка**: 415

**Исправление**: Проверить что parse_mode: "Markdown" установлен везде. Также убедиться что спецсимволы в тексте вопроса не ломают Markdown (экранирование `_`, `*`, `[`, `]`).

---

## Проблема 3: Слово "паттерн" непонятно для детей

**Симптомы**:
- "заметили интересный паттерн"
- "угадал свой паттерн"
- "твой паттерн — EA"

**Файлы**:
- `apps/bot/src/services/mirror.service.ts` — строка 154, 158
- `apps/bot/src/handlers/quiz.handler.ts` — строки 915, 923, 929

**Исправление**: Заменить "паттерн" на "профиль":
- "заметили интересный **профиль**"
- "угадал свой **профиль**"
- "твой **профиль** — ..."

---

## Проблема 4: Коды EA/ES показываются пользователю

**Симптом**: Пользователь видит "Ты выбрал ES, а твой паттерн — EA" — непонятные аббревиатуры.

**Файл**: `apps/bot/src/handlers/quiz.handler.ts`
**Строки**: 915, 923, 929

**Исправление**: Вместо кодов показывать человекочитаемые названия:
```typescript
// Было:
`твой паттерн — **${correctPattern}**`

// Стало:
`твой профиль — **${getPatternName(correctPattern)}**`
```

Функция `getPatternName()` уже существует в `mirror.service.ts` и возвращает названия типа "🎨 Творить с порядком".

---

## Проблема 5 (КРИТИЧЕСКАЯ): Неправильный расчёт результатов профориентации

**Симптом**: Все тестеры говорят, что результаты "не про них".

### 5.1 Неправильная формула Z-score нормализации

**Файл**: `apps/bot/src/services/results.service.ts`
**Строка**: 150

**Текущий код**:
```typescript
const percentile = Math.round(50 + zScore * 16.67);  // Линейная аппроксимация
```

**Проблема**: Это линейная аппроксимация, не соответствующая нормальному распределению.

**Исправление**: Использовать CDF нормального распределения с erf():
```typescript
// Правильная формула:
const cdf = 0.5 * (1 + erf(zScore / Math.SQRT2));
const percentile = Math.round(cdf * 100);
```

Функция `erf()` уже определена в `packages/shared/src/constants/riasec.ts`, но не используется!

### 5.2 Rating-вопросы учитывают только PRIMARY dimension

**Файл**: `apps/bot/src/services/results.service.ts`
**Строки**: 103-108

**Текущий код**:
```typescript
const rating = parseInt(answer.answerText, 10) || 0;
const dimension = question.primaryDimension as RIASECType;
rawScores[dimension] += rating;  // Только primary!
```

**Проблема**: Cross-loading игнорируется. Вопрос может влиять на несколько измерений.

**Исправление**: Для Rating-вопросов тоже использовать `scores` из опций, если они есть.

### 5.3 Нормы RIASEC могут быть некорректны

**Файл**: `packages/shared/src/constants/riasec.ts`
**Строки**: 9-16

Текущие нормы (mean/sd) взяты из O*NET, но нужно проверить соответствие формату наших вопросов (55 вопросов vs стандартные тесты).

---

## Порядок исправления

| # | Задача | Приоритет | Файл |
|---|--------|-----------|------|
| 1 | Добавить текст опций для BINARY вопросов | HIGH | `quiz.handler.ts` |
| 2 | Заменить "паттерн" → "профиль" | MEDIUM | `mirror.service.ts`, `quiz.handler.ts` |
| 3 | Скрыть коды EA/ES, показывать названия | MEDIUM | `quiz.handler.ts` |
| 4 | Исправить формулу Z-score (использовать erf) | CRITICAL | `results.service.ts` |
| 5 | Исправить Rating-вопросы (cross-loading) | HIGH | `results.service.ts` |
| 6 | Создать unit-тесты для алгоритма RIASEC | HIGH | `results.service.test.ts` |
| 7 | Проверить Markdown экранирование | LOW | `quiz.handler.ts` |

---

## Файлы для изменения

1. `apps/bot/src/handlers/quiz.handler.ts` — проблемы 1, 2, 3, 4
2. `apps/bot/src/services/results.service.ts` — проблема 5 (алгоритм)
3. `apps/bot/src/services/mirror.service.ts` — проблема 3 (текст)
4. `packages/shared/src/constants/riasec.ts` — импорт erf() в results.service

---

## Тестовые сценарии (нужно создать)

Для верификации алгоритма создадим 3 тестовых профиля:

### Профиль 1: "Чистый Реалист" (R-доминанта)
- Все ответы максимально в сторону R-измерения
- Ожидаемый результат: R > 80%, Holland code: R??

### Профиль 2: "Творческий исследователь" (IA-профиль)
- Ответы 70% на I-вопросы, 30% на A-вопросы
- Ожидаемый результат: I ~70%, A ~60%, архетип "IA"

### Профиль 3: "Сбалансированный" (все измерения ~50%)
- Равномерные ответы по всем категориям
- Ожидаемый результат: все измерения в диапазоне 40-60%

---

## Верификация

1. **UI-тесты**:
   - Запустить бота, пройти тест, проверить что BINARY вопросы показывают текст опций
   - Проверить что слово "паттерн" заменено на "профиль"
   - Проверить что коды EA/ES не показываются, только названия

2. **Тест алгоритма** (unit tests):
   - Создать тестовые профили с известными ответами
   - Проверить что raw scores считаются правильно
   - Проверить что normalizedScores соответствуют ожидаемым percentiles
   - Сравнить результаты до/после исправления

3. **Типы**:
   - `pnpm run type-check` должен пройти
   - `pnpm run build` должен пройти
